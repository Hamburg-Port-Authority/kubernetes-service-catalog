kube-state-metrics:
  image:
    registry: registry.k8s.io
    repository: kube-state-metrics/kube-state-metrics
  autosharding:
    enabled: false
  revisionHistoryLimit: 2
  prometheus:
    monitor:
      enabled: true
  selfMonitor:
    enabled: true