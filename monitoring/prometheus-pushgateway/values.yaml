prometheus-pushgateway:
  ingress:
    enanbled: true
    className: "nginx"

  securityContext:
    capabilities:
      drop:
        - ALL
    fsGroup: 65534
    runAsUser: 65534
    runAsNonRoot: true
    allowPrivilegeEscalation: false
    seccompProfile:
      type: RuntimeDefault

  persistentVolume:
    enabled: true